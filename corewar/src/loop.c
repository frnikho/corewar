/*
** EPITECH PROJECT, 2019
** loop.c
** File description:
** loop function
*/

#include "vm.h"

int loop(vm_t *vm)
{
    bool is_alive = true;

    if (vm->carry.flag == ERROR_CODE)
        return -1;
    while (is_alive && 0) {

    }
}